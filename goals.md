# Project Goals for Next Year

> ⚠️ **AI GENERATED CONTENT DISCLAIMER** ⚠️
> This document was automatically generated by AI assistance and may contain inaccuracies, errors, or incomplete information. 
> Please verify all technical details against the actual codebase before implementation.

## Abstract

This document outlines the comprehensive goals for the ECS (Entity Component System) language project over the next year. The project aims to develop a complete programming language with ECS as its core paradigm, featuring automatic runtime library linking, C semantic similarity, and extensive standard library support. The language will be demonstrated through a complex game example showcasing high interaction with many dynamic objects. Key development areas include implementing core ECS concepts, building a robust runtime engine, extending with C FFI capabilities, and creating web-based documentation. Secondary goals encompass advanced features like implicit parallelization, IR-level vectorization, and extended standard library functionality. Bonus objectives focus on improving developer experience through tutorials, syntax highlighting, project management tools, and language server support. The project leverages existing codebase infrastructure including engine-playground for core runtime implementation and lang/main for language demonstration examples.

## Main Goals

### Language with Basic ECS Elements
The language will implement core ECS concepts including entities, components, systems, and queries. Based on the codebase, we can see that the language already implements these concepts with syntax like:
- Entity creation with components: `rt.spawn! { Position((SCREEN_WIDTH/2, SCREEN_HEIGHT-50)) }`
- Component definitions: `component Position = float2`
- System definitions with queries: `system movement(entity: Velocity & mut Position)`
- Resource management: `resources (Score(0) SpawnInterval(60))`

### Language as Runtime Library
The language will provide a runtime library that automatically links during compilation. From the codebase we can see:
- The `engine-playground` directory contains the core runtime implementation
- The `lang` directory contains language-specific code that depends on the engine
- Cargo.toml files show dependencies on the engine components

### Semantic Similarity to C
The language will maintain semantic similarity to C, as evidenced by:
- Direct memory access patterns in the engine implementation
- Low-level component management using structures and references
- Efficient memory layout optimizations similar to C's approach

### Standard Library
The standard library will include:
- Mathematical functions, string operations, helper data types
- Time management, random number generators, file I/O
- Based on the engine-playground implementation, we see core data structures like `World`, `Archetype`, and component management that form the foundation for such features

### Window Library
The window library will use SDL3 or at minimum C FFI:
- The example code shows `BASIC_IMAGE` which suggests graphics support
- The engine implementation already has the foundation for rendering and window management through ECS

### Game Demonstrating Language Concepts
The system will demonstrate high interaction with many objects and dynamism:
- The main example in `lang/main` shows a complete game with physics, collision detection, and entity management
- Multiple systems working together: `spawnBullets`, `applyPullForce`, `bMove`
- Complex queries and entity interactions that demonstrate dynamic behavior

### Web Documentation
The documentation will describe language constructs, provide guidance, and include code examples:
- The README.md already shows examples of the language syntax
- The documentation will need to cover all ECS concepts properly

### C FFI Support
The language will support C FFI integration:
- The engine implementation already shows a system architecture that can be extended
- The project structure allows for integration with external libraries

## Secondary Goals

### Implicit Parallelization
The compiler will determine which code sections can run concurrently without user input:
- The engine shows parallelism handling in `World` with systems that can run in parallel
- The scheduling system (`scheduler main`) shows how systems can be ordered
- This requires more sophisticated analysis of component dependencies

### IR-Level Vectorization
The language will not rely on automatic vectorization from LLVM or link-time optimization:
- The engine implementation uses efficient memory layouts for vectorization
- The architecture is designed for performance at the component level
- This will require custom vectorization passes in the IR

### Extended Standard Library
The standard library will include:
- Parallelism support, testing capabilities, environment interaction
- Advanced algorithms and data structures, networking support
- The engine already provides the foundation with `World`, `Archetype`, and component management

### Dynamic Typed Structures
Some form of polymorphism with V-tables:
- The engine supports dynamic component management
- The architecture allows for dynamic entity behavior through component composition
- Requires extension to support runtime type information and dispatch

### Async Extension
The runtime will be extended with task scheduler:
- The engine already has scheduling capabilities
- This would involve adding async/await syntax and runtime task management

## Bonus Goals

### Tutorial
Web-based tutorial with example project solutions:
- The current examples in `lang/main` provide a foundation
- A more structured tutorial would expand on these concepts
- Could include interactive examples or web-based code editors

### Tree Sitter
Syntax highlighting in editors:
- The language has a defined syntax that could be parsed by Tree Sitter
- The parser-playground directory suggests parsing infrastructure exists
- This would make the language more accessible in editors

### Project Manager
Smarter CLI with standard project format, config files, and automatic documentation:
- The project structure shows potential for standardization
- Cargo.toml files show dependency management capabilities
- Could include automatic documentation generation based on code

### Language Server
Editor autocompletion and language features:
- The project structure supports language server development
- Parser infrastructure exists that could be extended
- Would provide features like intellisense and error highlighting